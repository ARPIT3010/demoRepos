pipeline {
    agent any
    stages {
        stage('Build') {
                steps {
                        echo 'Building the maven project'
                        sh 'cd ./demo'
			echo 'Moved the demo directory'
                        sh 'mvn clean intall'
			
                }
        }
   }
}   
